# Skopiuj do .env i uzupełnij WŁASNYMI danymi. Plik .env jest w .gitignore.
# Potrzebne tylko wtedy, gdy w config/site.json ustawisz login.mode = "auto".
# Tryb domyślny ("manual") NIE wymaga tych zmiennych — logujesz się ręcznie w oknie przeglądarki.

SITE_USER=
SITE_PASSWORD=
